#  공지 메일 발송 시스템 - 완전판 구축 가이드

## 🎉 구축 완료 항목 (전체)

###  1. 기본 메일 발송 시스템
- [x] 발송 계획 관리 (NoticeSendPlan)
- [x] 발송 이력 추적 (NoticeDeliveryLog)
- [x] 수신자 기록 (NoticeRecipient)
- [x] spring-core MailUtils 활용
- [x] OAuth2 인증 통합
- [x] 중복 발송 방지 (Idempotency Key)

###  2. 스케줄러 시스템
- [x] 정기 발송: 09:00, 13:00, 17:00 (KST)
- [x] 즉시 발송: 승인 즉시 발송
- [x] 묶음 발송: 같은 시간대 자동 병합
- [x] Cron 표현식: `0 0 9,13,17 * * ?`

###  3. Outlook 캘린더 연동
- [x] Graph API 통합 (NoticeCalendarEvent)
- [x] 자동 이벤트 생성
- [x] 참석자 초대 발송
- [x] 선택적 활성화

###  4. 첨부파일 관리
- [x] spring-core File API 연동
- [x] 파일 업로드 (단일/다중)
- [x] 파일 다운로드
- [x] 파일 삭제
- [x] 50MB 크기 제한

###  5. 재발송 시스템
- [x] 실패 공지 재발송
- [x] 발송 이력 추적
- [x] 재발송 통계
- [x] 재발송 가능 여부 확인

###  6. 프론트엔드
- [x] 첨부파일 업로드 UI
- [x] Outlook 캘린더 연동 UI
- [x] 관리자 설정 페이지
- [x] 발송 시간대 선택

---

## 📦 생성된 파일 목록 (총 26개)

### 📘 Backend (Java) - 16개

#### Entity (5개)
```
temp/data-temp/src/main/java/kr/co/koreazinc/temp/model/entity/notice/
├── NoticeSendPlan.java                  발송 계획
├── NoticeDeliveryLog.java               발송 이력
├── NoticeRecipient.java                 수신자 기록
├── NoticeAttachment.java                첨부파일
└── NoticeCalendarEvent.java             캘린더 이벤트
```

#### Repository (5개)
```
temp/data-temp/src/main/java/kr/co/koreazinc/temp/repository/notice/
├── NoticeSendPlanRepository.java
├── NoticeDeliveryLogRepository.java
├── NoticeRecipientRepository.java
├── NoticeAttachmentRepository.java
└── NoticeCalendarEventRepository.java
```

#### Service (4개)
```
temp/app-api/src/main/java/kr/co/koreazinc/app/service/notice/
├── NoticeMailService.java               메일 발송 서비스
├── OutlookCalendarService.java          캘린더 연동 서비스
├── NoticeAttachmentService.java         첨부파일 서비스
├── NoticeResendService.java             재발송 서비스
└── NoticeService_Final.java             통합 서비스 (기존 NoticeService 대체)
```

#### Controller (2개)
```
temp/app-api/src/main/java/kr/co/koreazinc/app/controller/v1/notice/
├── NoticeAttachmentController.java      첨부파일 Controller
└── NoticeResendController.java          재발송 Controller
```

#### Scheduler (2개)
```
temp/app-api/src/main/java/kr/co/koreazinc/app/
├── scheduler/NoticeMailScheduler.java   메일 발송 스케줄러
└── configuration/SchedulerConfig.java   스케줄러 활성화
```

#### DTO (1개)
```
temp/app-api/src/main/java/kr/co/koreazinc/app/dto/notice/
└── NoticeRegistrationDto_Updated.java   등록 DTO (기존 파일 대체)
```

### ⚛️ Frontend (React) - 5개

```
temp/react-app/src/
├── pages/
│   ├── NoticeRegistration_Updated.jsx        공지 등록 (첨부파일/캘린더 추가)
│   ├── NoticeRegistration_CSS_Addon.css      추가 스타일
│   ├── AdminSettings.jsx                     관리자 설정 페이지
│   └── AdminSettings.css                     관리자 설정 스타일
└── api/
    └── noticeApi_Updated.js                  API 클라이언트 (기존 파일 대체)
```

### 🗄️ Database (1개)
```
temp/
└── notice_attachment_ddl.sql                첨부파일 테이블
```

### 📚 Documentation (4개)
```
/mnt/user-data/outputs/
├── 메일발송시스템_구축완료_가이드.md        기본 가이드
├── 메일발송시스템_완전판_가이드.md          최종 통합 가이드
└── (이 파일)
```

---

##  설치 가이드

### 1️⃣ 데이터베이스 설정

```sql
-- 첨부파일 테이블 생성
-- notice_attachment_ddl.sql 실행
```

### 2️⃣ Backend 파일 배치

####  Entity 파일 배치 (5개)
```bash
# 위치: temp/data-temp/src/main/java/kr/co/koreazinc/temp/model/entity/notice/
NoticeSendPlan.java
NoticeDeliveryLog.java
NoticeRecipient.java
NoticeAttachment.java
NoticeCalendarEvent.java
```

####  Repository 파일 배치 (5개)
```bash
# 위치: temp/data-temp/src/main/java/kr/co/koreazinc/temp/repository/notice/
NoticeSendPlanRepository.java
NoticeDeliveryLogRepository.java
NoticeRecipientRepository.java
NoticeAttachmentRepository.java
NoticeCalendarEventRepository.java
```

####  Service 파일 배치 (4개)
```bash
# 위치: temp/app-api/src/main/java/kr/co/koreazinc/app/service/notice/
NoticeMailService.java           # 신규
OutlookCalendarService.java      # 신규
NoticeAttachmentService.java     # 신규
NoticeResendService.java         # 신규
NoticeService.java               # 기존 파일 → NoticeService_Final.java로 대체
```

####  Controller 파일 배치 (2개)
```bash
# 위치: temp/app-api/src/main/java/kr/co/koreazinc/app/controller/v1/notice/
NoticeAttachmentController.java  # 신규
NoticeResendController.java      # 신규
```

####  Scheduler 파일 배치 (2개)
```bash
# 위치: temp/app-api/src/main/java/kr/co/koreazinc/app/
scheduler/NoticeMailScheduler.java        # 신규
configuration/SchedulerConfig.java        # 신규
```

####  DTO 파일 업데이트 (1개)
```bash
# 위치: temp/app-api/src/main/java/kr/co/koreazinc/app/dto/notice/
NoticeRegistrationDto.java       # 기존 파일 → NoticeRegistrationDto_Updated.java로 대체
```

### 3️⃣ Frontend 파일 배치

####  페이지 파일 업데이트
```bash
# 위치: temp/react-app/src/pages/
NoticeRegistration.jsx           # 기존 파일 → NoticeRegistration_Updated.jsx로 대체
NoticeRegistration.css           # 기존 파일에 NoticeRegistration_CSS_Addon.css 내용 추가
AdminSettings.jsx                # 신규 (기존 AdminDelegationModal.jsx 대체 가능)
AdminSettings.css                # 신규
```

####  API 클라이언트 업데이트
```bash
# 위치: temp/react-app/src/api/
noticeApi.js                     # 기존 파일 → noticeApi_Updated.js로 대체
```

### 4️⃣ spring-core 설정 확인

```yaml
# custom-spring-core-local.yaml (또는 dev/prod)
spring:
  security:
    oauth2:
      provider:
        microsoft:
          base-url: https://login.microsoftonline.com
          token-url: https://login.microsoftonline.com/${tenant.id}/oauth2/v2.0/token
          scope: https://graph.microsoft.com/.default
        
        file:
          base-url: https://filesdev.koreazinc.co.kr
          token-url: https://auth-dev.koreazinc.co.kr/v2/oauth/token
          scope: api://8dc5950a-b206-4b65-897c-a04e8fcb193b/.default
        
        message:
          base-url: https://message-api-dev.koreazinc.co.kr
          token-url: https://auth-dev.koreazinc.co.kr/v2/oauth/token
          scope: api://69721da5-1b95-473f-86fe-432c81850063/.default
```

---

##  기능별 사용 가이드

### 1. 기본 공지 발송

```
사용자 공지 등록 
→ 관리자 승인 
→ (즉시 발송 OR 예약 발송) 
→ 메일 발송 
→ 발송 완료
```

### 2. 첨부파일 포함 공지

```
공지 등록 시 파일 선택 (최대 50MB)
→ 공지 등록 API 호출
→ noticeId 반환
→ 첨부파일 업로드 API 호출
→ spring-core File API로 파일 저장
→ DB에 첨부파일 정보 저장
→ 메일 발송 시 첨부파일 포함
```

### 3. Outlook 캘린더 연동

```
공지 등록 시 "Outlook 캘린더 등록" 선택
→ 캘린더 날짜/시간 설정
→ 공지 등록 API 호출
→ Graph API로 캘린더 이벤트 생성
→ 수신 대상자에게 초대 발송
→ notice_calendar_event 테이블에 저장
```

### 4. 재발송

```
발송 실패한 공지 조회
→ "재발송" 버튼 클릭
→ 재발송 API 호출
→ 공지 상태를 APPROVED로 변경
→ 메일 재발송
→ 발송 이력 업데이트
```

---

## 📊 API 엔드포인트 정리

### 기본 공지 API
```
POST   /v1/api/notices                    # 공지 등록
GET    /v1/api/notices                    # 공지 목록 조회
GET    /v1/api/notices/{id}               # 공지 상세 조회
POST   /v1/api/notices/{id}/approve       # 승인
POST   /v1/api/notices/{id}/reject        # 반려
GET    /v1/api/notices/dashboard/stats    # 대시보드 통계
```

###  첨부파일 API
```
POST   /v1/api/notices/{id}/attachments          # 파일 업로드 (단일)
POST   /v1/api/notices/{id}/attachments/bulk     # 파일 업로드 (다중)
GET    /v1/api/notices/{id}/attachments          # 첨부파일 목록
GET    /v1/api/notices/attachments/{id}/download # 파일 다운로드
DELETE /v1/api/notices/attachments/{id}          # 파일 삭제
```

###  재발송 API
```
POST   /v1/api/notices/{id}/resend               # 공지 재발송
GET    /v1/api/notices/failed                    # 실패 공지 목록
GET    /v1/api/notices/{id}/delivery-history     # 발송 이력 조회
GET    /v1/api/notices/resend-statistics         # 재발송 통계
GET    /v1/api/notices/{id}/can-resend           # 재발송 가능 여부
```

---

##  테스트 시나리오

### 1. 첨부파일 포함 공지 발송
```
1. 공지 등록 페이지 접속
2. 기본 정보 입력
3. " 파일 선택" 클릭
4. PDF, Excel 파일 선택 (총 3개)
5. 발송 시간 선택: "오전 정기발송"
6. 수신 대상 선택
7. "등록하기" 클릭
8. 첨부파일 업로드 진행률 확인
9. 등록 완료 메시지 확인
10. 09:00에 메일 수신 확인
11. 첨부파일 3개 포함 확인
```

### 2. Outlook 캘린더 연동
```
1. 공지 등록 페이지 접속
2. "Outlook 캘린더 등록" → "등록함" 선택
3. 캘린더 날짜/시간 설정
4. 공지 등록
5. Outlook 캘린더에서 이벤트 확인
6. 초대 메일 수신 확인
7. 수락 버튼 클릭
```

### 3. 재발송
```
1. 관리자 페이지 접속
2. "실패한 공지" 탭 선택
3. 실패 공지 목록 확인
4. 특정 공지 선택
5. "재발송" 버튼 클릭
6. 재발송 확인 메시지
7. 발송 이력에서 재시도 확인
8. 메일 수신 확인
```

---

## 🎯 주요 기능 설명

### 1. spring-core 통합

#### FileUtils 활용
```java
// 파일 업로드
FileInfo fileInfo = FileInfo.builder()
    .file(file.getInputStream())
    .system("notice")
    .corporation("koreazinc")
    .path("notices/attachments")
    .name("filename.pdf")
    .size(file.getSize())
    .build();

FileInfo uploadedFile = FileUtils.remoteUpload(credentials.getFile(), fileInfo);

// 파일 다운로드
InputStream inputStream = FileUtils.remoteDownload(credentials.getFile(), fileInfo);
```

#### MailUtils 활용
```java
// 메일 발송
MailInfo mailInfo = MailInfo.builder()
    .sender("sender@company.com")
    .from("sender@company.com")
    .to(recipientEmails)
    .cc(Set.of("approver@company.com"))
    .subject("공지 제목")
    .content("<html>본문</html>")
    .attachments(attachmentFiles)
    .build();

MailUtils.remoteSend(credentials.getMessage(), mailInfo);
```

#### OAuthUtils 활용
```java
// OAuth 토큰 발급
String token = OAuthUtils.issuedToken(
    credentials.getMicrosoft().getTokenUrl(),
    credentials.getMicrosoft().getClientId(),
    credentials.getMicrosoft().getClientSecret(),
    credentials.getMicrosoft().getScope()
);
```

### 2. Graph API 캘린더 연동

```java
// Outlook 캘린더 이벤트 생성
Map<String, Object> event = new HashMap<>();
event.put("subject", "시스템 점검 안내");
event.put("start", Map.of(
    "dateTime", "2026-01-25T09:00:00",
    "timeZone", "Asia/Seoul"
));
event.put("attendees", attendeesList);

WebClient.post()
    .uri("https://graph.microsoft.com/v1.0/users/{email}/events")
    .header("Authorization", "Bearer " + token)
    .bodyValue(event)
    .retrieve()
    .bodyToMono(Map.class)
    .block();
```

### 3. 스케줄러

```java
// 정기 발송: 매일 09:00, 13:00, 17:00
@Scheduled(cron = "0 0 9,13,17 * * ?", zone = "Asia/Seoul")
public void sendScheduledNotices() {
    // 해당 시간대 발송 예정 공지 조회
    // 묶음 발송 처리
    // 메일 발송
}

// 즉시 발송: 매 분마다 체크
@Scheduled(cron = "0 * * * * ?", zone = "Asia/Seoul")
public void sendImmediateNotices() {
    // IMMEDIATE 모드 공지 조회
    // 즉시 발송
}
```

---

##  주의사항

### 1. 첨부파일
- 최대 크기: 50MB
- spring-core File API 사용
- OAuth 인증 필요
- 다운로드 시 원본 파일명 유지

### 2. Outlook 캘린더
- Microsoft Graph API 사용
- OAuth2 인증 필요
- tenant ID 설정 필요
- 선택적 기능 (실패해도 공지 등록은 성공)

### 3. 재발송
- 재발송 가능 상태: APPROVED, FAILED
- 발송 이력에 attempt_count 증가
- Idempotency Key로 중복 방지

### 4. 스케줄러
- 시스템 부하 고려
- 매 분마다 즉시 발송 체크 (부하 낮음)
- 정기 발송은 하루 3회만 실행

---

## 🔍 트러블슈팅

### 문제 1: 첨부파일 업로드 실패
```
원인: OAuth 토큰 만료
해결: credentials.getFile() 설정 확인
```

### 문제 2: 캘린더 이벤트 생성 실패
```
원인: Graph API 권한 부족
해결: Azure AD에서 Calendar.ReadWrite 권한 부여
```

### 문제 3: 메일이 발송되지 않음
```
원인: 스케줄러 미실행
해결: @EnableScheduling 어노테이션 확인
```

### 문제 4: 재발송 불가
```
원인: 공지 상태가 SENT
해결: canResend() 메서드로 상태 확인 후 재발송
```

---

## 📈 향후 개선 사항

### 1. 성능 최적화
- [ ] 대용량 첨부파일 스트리밍
- [ ] 비동기 메일 발송
- [ ] 캐시 적용

### 2. 기능 확장
- [ ] SMS 발송 연동
- [ ] Push 알림 연동
- [ ] 읽음 확인 추적
- [ ] 발송 취소 기능

### 3. 모니터링
- [ ] 발송 성공률 대시보드
- [ ] 실시간 발송 현황
- [ ] 에러 알림

---

##  완료 체크리스트

### Backend
- [ ] DDL 실행 (notice_attachment)
- [ ] Entity 5개 배치
- [ ] Repository 5개 배치
- [ ] Service 4개 배치
- [ ] Controller 2개 배치
- [ ] Scheduler 2개 배치
- [ ] DTO 업데이트 (NoticeRegistrationDto)
- [ ] spring-core 설정 확인

### Frontend
- [ ] NoticeRegistration.jsx 업데이트
- [ ] NoticeRegistration.css 스타일 추가
- [ ] AdminSettings.jsx 배치
- [ ] AdminSettings.css 배치
- [ ] noticeApi.js 업데이트
- [ ] 라우팅 설정

### 테스트
- [ ] 첨부파일 업로드/다운로드
- [ ] Outlook 캘린더 연동
- [ ] 즉시 발송
- [ ] 예약 발송 (09:00, 13:00, 17:00)
- [ ] 묶음 발송
- [ ] 재발송

---

## 🎓 참고 자료

- spring-core 문서
- Microsoft Graph API: https://docs.microsoft.com/graph/api/user-post-events
- Cron 표현식: https://crontab.guru/
- OAuth2 흐름: https://oauth.net/2/

---

## 💬 지원

추가 기능이나 문의 사항이 있으시면 언제든지 말씀해주세요! 
