#  Outlook 캘린더 테스트 모드 가이드

##  중요: 현재 상태

**기존 코드는 실제 Outlook 캘린더에 이벤트를 생성합니다!**

```java
// OutlookCalendarService.java - 79번 라인
Map<String, Object> response = WebClient.builder()
    .baseUrl(GRAPH_API_BASE)
    .build()
    .post()
    .uri("/users/" + senderEmail + "/events")  // 👈 실제 Graph API 호출!
    .header("Authorization", "Bearer " + token)
    .bodyValue(eventBody)
    .retrieve()
    .bodyToMono(Map.class)
    .block();
```

**테스트 없이 공지를 등록하면:**
-  실제 사용자들의 Outlook 캘린더에 이벤트가 생성됨
-  모든 참석자에게 초대 메일이 발송됨
-  삭제하려면 수동으로 일일이 삭제해야 함

---

##  해결책: 캘린더 테스트 모드 추가

### 📦 업데이트된 파일 (2개)

#### 1. MailTestProperty.java (캘린더 설정 추가)
```java
/**
 * Outlook 캘린더 테스트 모드
 * true: 실제 캘린더 이벤트 생성 안함, 로그만 출력
 * false: 실제 캘린더 이벤트 생성
 */
private Boolean calendarTestMode = true;
```

#### 2. OutlookCalendarService_WithTestMode.java (테스트 모드 적용)
```java
// 테스트 모드 체크
if (mailTestProperty.getCalendarTestMode()) {
    //  로그만 출력
    logCalendarEventForTest(...);
    eventId = "TEST_EVENT_" + UUID.randomUUID();
} else {
    //  실제 Graph API 호출
    WebClient.post().uri("/users/" + email + "/events")...
}
```

---

## 🎯 설정 방법

### application.yaml에 설정 추가

```yaml
notice:
  mail:
    # 메일 테스트 모드
    test-mode: true
    
    #  캘린더 테스트 모드 (새로 추가)
    calendar-test-mode: true
    
    # 화이트리스트 (선택사항)
    whitelist-mode: false
    whitelist-emails:
      - your.email@company.com
```

---

##  테스트 시나리오

### 시나리오 1: 완전 안전 테스트 (추천) ⭐

**설정:**
```yaml
notice:
  mail:
    test-mode: true              # 메일 발송 안함
    calendar-test-mode: true     # 캘린더 이벤트 생성 안함
```

**동작:**
-  메일 발송 안됨 (로그만 출력)
-  캘린더 이벤트 생성 안됨 (로그만 출력)
-  데이터베이스에는 정상 저장
-  공지 상태는 SENT로 변경

**로그 예시:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 [캘린더 테스트 모드] Outlook 이벤트 생성 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 공지 ID: 1
 공지 제목: 시스템 점검 안내
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 이벤트 제목: 시스템 점검 안내
 시작 시간: 2026-01-25 09:00
 종료 시간: 2026-01-25 10:00
 시간대: Asia/Seoul
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 주최자 (Organizer): admin@company.com
 캘린더 소유자: admin@company.com
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 참석자 (Attendees): 15 명
 참석자 목록:
    user1@company.com
    user2@company.com
    user3@company.com
   ... (전체 목록)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 이벤트 본문:
<html><body>시스템 점검으로 인해...</body></html>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 알림: 15분 전
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Graph API 엔드포인트:
   POST https://graph.microsoft.com/v1.0/users/admin@company.com/events
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 캘린더 테스트 모드이므로 실제 이벤트는 생성되지 않았습니다!
 실제 이벤트 생성을 원하시면 application.yaml에서
 notice.mail.calendar-test-mode: false 로 설정하세요
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 [캘린더 테스트 모드] 실제 이벤트는 생성되지 않았습니다 
 Outlook 캘린더 이벤트 정보 저장 완료: eventId=TEST_EVENT_xxx-xxx-xxx
```

---

### 시나리오 2: 메일만 실제 발송 테스트

**설정:**
```yaml
notice:
  mail:
    test-mode: false              # 메일은 실제 발송
    calendar-test-mode: true      # 캘린더는 테스트
    whitelist-mode: true
    whitelist-emails:
      - your.email@company.com
```

**동작:**
-  본인에게만 메일 발송
-  캘린더 이벤트는 생성 안됨

---

### 시나리오 3: 캘린더만 실제 생성 테스트

**설정:**
```yaml
notice:
  mail:
    test-mode: true               # 메일은 테스트
    calendar-test-mode: false     # 캘린더는 실제 생성
    whitelist-mode: true
    whitelist-emails:
      - your.email@company.com
```

**동작:**
-  메일은 발송 안됨
-  본인의 Outlook 캘린더에만 이벤트 생성

---

### 시나리오 4: 실제 운영 (모든 테스트 완료 후)

**설정:**
```yaml
notice:
  mail:
    test-mode: false              # 메일 실제 발송
    calendar-test-mode: false     # 캘린더 실제 생성
    whitelist-mode: false
```

**동작:**
-  메일 실제 발송
-  캘린더 이벤트 실제 생성
-  모든 사용자에게 적용

---

##  전체 흐름

```
┌─────────────────────────────────────────────────────┐
│          공지 등록 (Outlook 캘린더 등록함)             │
│  outlookCalendar: { register: true, eventDate: ... } │
└────────────────┬────────────────────────────────────┘
                 │
                 ↓
         NoticeService.createNotice()
                 │
                 ↓
    OutlookCalendarService.createCalendarEvent()
                 │
         ┌───────┴───────┐
         ↓               ↓
  calendar-test-mode  calendar-test-mode
     = true              = false
         ↓               ↓
    로그만 출력      Graph API 호출
  (이벤트 안 생성)  (실제 이벤트 생성)
         ↓               ↓
    TEST_EVENT_ID    실제 EVENT_ID
         ↓               ↓
    데이터베이스 저장 (동일)
         ↓
   notice_calendar_event 테이블
```

---

## 🔄 메일 + 캘린더 조합 테스트

### 조합 1: 모두 테스트 (가장 안전) ⭐
```yaml
test-mode: true
calendar-test-mode: true
```
- 메일: 로그만
- 캘린더: 로그만
- **첫 테스트 시 필수!**

### 조합 2: 메일만 실제
```yaml
test-mode: false
calendar-test-mode: true
whitelist-mode: true
```
- 메일: 본인에게만 발송
- 캘린더: 로그만

### 조합 3: 캘린더만 실제
```yaml
test-mode: true
calendar-test-mode: false
```
- 메일: 로그만
- 캘린더: 본인에게만 생성

### 조합 4: 모두 실제 (운영)
```yaml
test-mode: false
calendar-test-mode: false
whitelist-mode: false
```
- 메일: 전체 발송
- 캘린더: 전체 생성

---

## 📊 기능 비교표

| 기능 | 테스트 모드 | 실제 모드 |
|------|-----------|----------|
| **메일 발송** | 로그만 출력 | 실제 메일 발송 |
| **캘린더 이벤트** | 로그만 출력 | Graph API 호출 |
| **데이터베이스 저장** |  저장됨 |  저장됨 |
| **공지 상태 변경** |  SENT |  SENT |
| **발송 이력** |  기록됨 |  기록됨 |
| **Outlook 초대** |  안 나감 |  발송됨 |

---

## 🎯 테스트 체크리스트

### 1단계: 캘린더 테스트 모드 확인
- [ ] MailTestProperty.java 배치
- [ ] OutlookCalendarService.java 교체
- [ ] application.yaml 설정 추가
- [ ] 애플리케이션 재시작

### 2단계: 로그 확인
- [ ] 공지 등록 (outlookCalendar.register: true)
- [ ] 로그에서 캘린더 정보 확인
- [ ] 참석자 목록 확인
- [ ] 이벤트 시간 확인

### 3단계: 본인에게만 테스트
- [ ] calendar-test-mode: false
- [ ] whitelist-mode: true (본인 이메일만)
- [ ] 본인 Outlook에서 이벤트 확인
- [ ] 초대 메일 수신 확인

### 4단계: 실제 운영
- [ ] 모든 테스트 완료 확인
- [ ] 설정 변경
- [ ] 전체 사용자에게 적용

---

## 💡 추가 정보

### Outlook 캘린더 이벤트 구성 요소

```json
{
  "subject": "시스템 점검 안내",
  "start": {
    "dateTime": "2026-01-25T09:00:00",
    "timeZone": "Asia/Seoul"
  },
  "end": {
    "dateTime": "2026-01-25T10:00:00",
    "timeZone": "Asia/Seoul"
  },
  "attendees": [
    {
      "emailAddress": { "address": "user1@company.com" },
      "type": "required"
    }
  ],
  "isReminderOn": true,
  "reminderMinutesBeforeStart": 15
}
```

### Graph API 엔드포인트

```
POST https://graph.microsoft.com/v1.0/users/{userEmail}/events
Authorization: Bearer {token}
Content-Type: application/json
```

---

## 🆘 FAQ

### Q1: 캘린더 이벤트는 언제 생성되나요?
**A:** 공지 등록 시점에 즉시 생성됩니다. (승인과 무관)

### Q2: 테스트 모드에서도 데이터베이스에 저장되나요?
**A:** 네, `notice_calendar_event` 테이블에 정상 저장됩니다.

### Q3: 참석자 목록은 어떻게 결정되나요?
**A:** 공지의 수신 대상자(법인/부서/개인)와 동일합니다.

### Q4: 캘린더 이벤트를 삭제하려면?
**A:** 테스트 모드 권장! 실제 생성된 경우 Outlook에서 수동 삭제 필요.

### Q5: 메일과 캘린더를 따로 테스트할 수 있나요?
**A:** 네! test-mode와 calendar-test-mode를 독립적으로 설정 가능.

---

##  주의사항

### 1. 테스트 없이 실행 금지!
```yaml
#  위험한 설정 (테스트 없이 운영)
test-mode: false
calendar-test-mode: false
whitelist-mode: false
```
→ 모든 사용자에게 메일 + 캘린더 초대 발송!

### 2. 환경별 설정 분리

#### 개발 환경 (application-dev.yaml)
```yaml
notice:
  mail:
    test-mode: true
    calendar-test-mode: true
```

#### 운영 환경 (application-prod.yaml)
```yaml
notice:
  mail:
    test-mode: false
    calendar-test-mode: false
```

### 3. Graph API 권한 확인
- Calendar.ReadWrite 권한 필요
- Azure AD 앱 등록에서 확인

---

##  배포 가이드

### 1. 파일 배치
```
temp/app-api/src/main/java/kr/co/koreazinc/app/
├── configuration/
│   └── MailTestProperty.java  (업데이트)
└── service/notice/
    └── OutlookCalendarService.java  (교체)
```

### 2. 설정 추가
```yaml
# application.yaml
notice:
  mail:
    calendar-test-mode: true  # ← 추가
```

### 3. 테스트
- 로그 확인
- 본인에게만 테스트
- 운영 전환

---

##  최종 정리

1. **캘린더도 메일처럼 테스트 모드 적용됨**
2. **로그로 모든 정보 확인 가능**
3. **독립적으로 테스트 가능 (메일 따로, 캘린더 따로)**
4. **안전하게 단계별 테스트**

이제 안심하고 캘린더 기능도 테스트하세요! 🎉
